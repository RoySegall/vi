export const keyIndexForZero = (rows) => {
};

export const isDraggable = (rows, number) => {
};

export const switchNumberWithZero = (rows, numbers) => {
};

export const isGameSolved = (rows) => {
};
